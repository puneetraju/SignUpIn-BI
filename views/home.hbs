<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Credentials</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f7f7f7;
            margin: 0;
        }
        .card {
            max-width: 500px; 
            width: 100%;
            background: #fff;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 30px;
            box-sizing: border-box;
            text-align: center;
        }
        h1 {
            margin-bottom: 20px; 
        }
        .btn-logout {
            margin-top: 20px;
            padding: 12px 24px;
            border: none;
            border-radius: 4px;
            background-color: #dc3545;
            color: #fff;
            font-size: 18px;
            cursor: pointer;
            text-decoration: none;
            display: inline-block; 
        }
        .btn-logout:hover {
            background-color: #c82333;
        }
    </style>
</head>
<body>
    <div class="card">
        <h1>You have been verified and logged in</h1>
        <a href="/logout" class="btn-logout">Logout</a>
    </div>
</body>
</html>
